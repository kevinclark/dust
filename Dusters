Block stuff
===========
calling yield when you attach the block to an arg

def foo(&block)
  yield
end

check for existence of the block when capturing and calling
def foo(&block)
  # if block
  # if !block.nil?
  # unless block.nil?
end

Hash stuff
==========
options = {}
options[:a] = 4
options[:b] = 5
options[:c] = 6

Methods
=======
empty methods

Other
=====
NoMethodException on NilClass
if x && !x.nil? # tautology
if x && x.nil?  # contradiction